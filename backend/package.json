{
  "name": "fideliza-corretor-backend",
  "version": "0.1.0",
  "description": "Backend do Fideliza Corretor (Fase 1 — CRUD de clientes com score de completude).",
  "main": "src/server.js",
  "scripts": {
    "dev": "node --watch src/server.js",
    "start": "node src/server.js",
    "cron": "node scripts/run-jobs.js",
    "test": "node --test"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.45.0",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "multer": "^2.2.0"
  }
}
